-- One-time: promote an existing user to super_admin (run in Supabase SQL Editor)
-- Replace the email with your account (register on patient site first if needed).

-- UPDATE users SET role = 'super_admin' WHERE LOWER(email) = LOWER('your@email.com');
